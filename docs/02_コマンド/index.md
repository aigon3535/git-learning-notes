# 目次

ここではgitの操作・具体的なコマンドを紹介します。使用頻度に合わせて、3つの階級ごとに詳細を説明します。

## おすすめ学習順

実務で使う頻度と、初学者がつまずきにくい流れを優先した順番です。
今後、コマンドごとの個別ページを作る時も、この順番をベースにします。

`status` `clone` `pull` `branch` `switch` `add` `commit` `push` `diff` [`stash`](03_stash.md) `merge` `restore` `reset` `rebase`

### [初級編](01_初級編.md)

基本のコマンドです。リポジトリの取得から、ファイルの更新、リポジトリへの反映と、Gitを使った一連の作業に必須な操作となり、これらを習得すれば、Gitを使った開発に参加はできます。

`status` `clone` `pull` `branch` `switch` `add` `commit` `push` `diff`


### [中級編](02_中級編.md)


[`stash`](03_stash.md) `merge` `restore` `reset` `rebase`

### 上級編（*工事中）
